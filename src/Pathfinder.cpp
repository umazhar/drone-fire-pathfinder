#include "../include/Pathfinder.hpp"

std::vector<std::pair<int,int>> Pathfinder::findPath(
    const GridMap& map, 
    int startX, int startY, 
    int goalX, int goalY
) {
    std::vector<std::pair<int,int>> path;

    // For now, you could do a trivial BFS or even a direct line (if no obstacles).
    // Just fill in your pathfinding logic here.
    // Then return the path.
    return path;
}
